[EVENT-REMINDER]
; for names without '/' or '\' the programme will look in the main dir, otherwise it will interpret it as a full path
; use \\ on Windows
event-file=events.sample
; 1 to log number of events found per execution
log-day=1
